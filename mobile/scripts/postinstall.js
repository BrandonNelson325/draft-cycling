/**
 * Post-install script placeholder.
 *
 * Previously this patched expo-modules-core/package.json "main" to "index.js",
 * but that file is a null stub. The correct "main" is "src/index.ts" (the
 * published default), which Metro resolves natively.
 */
console.log('[postinstall] No patches needed.');
